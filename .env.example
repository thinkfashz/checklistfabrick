# Configuración de Entorno para Casas Fabris

# Puerto del servidor (fijo en 3000 por la plataforma)
PORT=3000

# Entorno (production o development)
NODE_ENV=development

# Firebase Config (se inyecta automáticamente en AI Studio)
# GEMINI_API_KEY=
